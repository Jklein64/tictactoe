{
    "name": "tictactoe",
    "version": "1.0.0",
    "engines": {
        "node": "13.x",
        "yarn": "1.x"
    },
    "license": "MIT",
    "scripts": {
        "build": "cd client && yarn install && yarn run build",
        "postinstall": "yarn install --prefix server && yarn install --prefix client",
        "start": "node server"
    }
}