{
    "name": "tictactoe",
    "version": "1.0.0",
    "engines": {
        "node": "13.x"
    },
    "main": "./server/index.js",
    "license": "MIT",
    "scripts": {
        "build": "ROOT=\"$(pwd)\" && cd ./server && npm install && npm run build && cd $ROOT && cd ./client && npm install && npm run build && cd $ROOT",
        "start": "npm run build && cd ./server && npm run start"
    }
}